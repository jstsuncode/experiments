box: nuzz/sightmachine
build:
  steps:
    - script:
        name: test
        code: |
          cd school
          python test_student.py
    - script:
        name: test with code coverage
        code: |
          nosetests --with-coverage --cover-package=school
       #  cd school
       #  coverage run test_student.py
       #  coverage report
    - script:
        name: Coveralls.io
        code: |
          nosetests --with-coverage
          coveralls
