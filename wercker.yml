box: nuzz/sightmachine
steps:
  - wercker/pip-install@0.0.6
build:
  steps:
    - script:
        name: test with code coverage
        code: |
          nosetests --with-coverage
  after-steps:
    - script:
        name: Coveralls.io
        code: |
          coveralls
